/*alert() function is a simple function to dispaly a massage on browser window in 
 display bar or alert box.*/

/*prompt() method display a dilog box  that prompt the user input. the prompt
 method return the input value if the user click ok otherwise it return null.
 // prompt always return a string value*/

//parseint() is used to convert the string value into a integer value.
let Num2 =prompt("Enter the number");
        let y=parseInt(Num2); 
// In this code take a user value through prompt function and that value convert in integer valu
//using parseInt()function because prompt return always string value.


